# Secrets — NEVER commit. Real values live in .env (ignored) or a vault.
.env
.env.*
!.env.example
*.pem
*.key
aws-credentials*
**/credentials

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Audio fixtures / recordings (synthetic only, but keep large media out of git)
*.wav
*.mp3
*.m4a
data/
recordings/

# OS / editor
.DS_Store
.idea/
.vscode/
